/**
 * Rôles utilisateurs (Auth0 RBAC) — voir README racine.
 */

export type UserRole = 'USER' | 'ADMIN';

/** Plans d'abonnement (slugs) */
export type PlanSlug = 'VISITEUR' | 'APPRENTI' | 'COMPAGNON' | 'REUSSITE';
