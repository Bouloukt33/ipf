FROM node:20-alpine

WORKDIR /app

# Installer les dépendances
COPY package*.json ./
RUN npm install

# Le code source est monté via volume (hot-reload)
COPY . .

EXPOSE 5173

# --host expose Vite à l'extérieur du container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]